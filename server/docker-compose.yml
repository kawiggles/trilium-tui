services:
  trilium:
    image: triliumnext/trilium:latest
    container_name: trilium-server
    restart: unless-stopped
    environment:
      - TRILIUM_DATA_DIR=/home/node/trilium-data
    ports:
      - '10000:8080'
    volumes:
      - ~/trilium-data:/home/node/trilum-data
